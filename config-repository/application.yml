# 🏗️ ARKA Config Repository - Application Default
# Configuración compartida para todos los microservicios
# Este archivo contiene configuraciones comunes que aplican a todos los servicios

# Spring Configuration (común para todos)
spring:
  # Base configuration for all services
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1800000
  
  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# Configuración de Eureka común
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
    healthcheck:
      enabled: true
    lease:
      duration: 30
    registry-fetch-interval: 30
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 20

# Configuración de management común
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,configprops,refresh
  endpoint:
    health:
      show-details: always
    refresh:
      enabled: true

# Security Configuration (común)
security:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration: 86400000  # 24 hours

# Logging Configuration (común)
logging:
  level:
    com.arkavalenzuela: DEBUG
    org.springframework.security: INFO
    org.springframework.cloud: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    refresh:
      enabled: true
  endpoints:
    web:
      base-path: /actuator
      cors:
        allowed-origins: "*"
        allowed-methods: GET,POST

# Configuración de logging común
logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
  level:
    org.springframework.cloud: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO

# Configuración de Spring común
spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: America/Bogota
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
